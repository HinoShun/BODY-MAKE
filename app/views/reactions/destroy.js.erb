<!-- クリック、データ削除後、HTML入れ替え --><!-- クリック、データ登削除後、HTML入れ替え -->
document.getElementById('reaction-btn<%= @tweet.id %>').innerHTML = "<%= j(render partial: 'reactions/reaction', locals: {tweet: @tweet}) %>"